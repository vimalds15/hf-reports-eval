const CanvasDefaultText = () => {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-lg font-semibold text-gray-500">
        Select a Report or Metric to Display
      </p>
    </div>
  );
};

export default CanvasDefaultText;
